const initialState = {
  profile: null,
  profiles: null,
  loading: false
};

export default function(state = innitialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
